[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prisma-function"
version = "1.0.0"
description = "Windows desktop application for processing official PRISMA Export CSV files."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
